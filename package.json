{
  "name": "bet365-stats",
  "version": "1.0.0",
  "description": "* NodeJS\r * npm | yarn\r * npx",
  "scripts": {
    "start": "npm i && pm2 delete all && npm run build && pm2 start build/index.js",
    "start:dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/puppeteer": "^5.4.5",
    "chromedriver": "^99.0.0",
    "dotenv": "^16.0.0",
    "dotnet": "^1.1.4",
    "mongoose": "^6.2.6",
    "playwright": "^1.20.0",
    "readline": "^1.3.0",
    "telegram": "^2.26.22"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.0",
    "@types/node": "^17.0.45",
    "nodemon": "^2.0.19",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  }
}
